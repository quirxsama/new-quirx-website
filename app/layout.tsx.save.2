OOOOOOOOOOOOOOOOOOOBBOOOOOOOOOOOBOBOOimport type { Metadata } from "next";
import { Inter, Poppins, Dancing_ScriOBOOOpt } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });
const poppins = Poppins({ 
  weight: ['400', '500', '600'],
  subsets: ['latin'],
  variable: '--font-poppins'
});
const dancingScript = Dancing_Script({
  weight: ['400', '500'],
  subsets: ['latin'],
  variable: '--font-dancing'
});

export const metadata: Metadata = {
  title: "Quirx'in Köşesi",
  description: "QuirxKaan, kim ki bu? Neredendir neler yapar, neler sever!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="tr">
      <body className={`${inter.className} ${poppins.variable} ${dancingScript.variable} bg-gradient-to-br from-gray-900 to-black text-white min-h-screen`}>
        <div className="fixed inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-purple-900/20 via-transparent to-transparent pointer-events-none"></div>
        {children}
      </body>
    </html>
  );
} 
